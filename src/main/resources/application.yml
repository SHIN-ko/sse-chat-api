spring:
  application:
    name: sse-chat-api

server:
  port: 8080

app:
  model:
    base-url: http://localhost:8000/v1   # ? ???? ?? ??? ?? ? localhost ??
    endpoint: /completions           # ?? URL = base-url + endpoint
    name: local-llama                     # compose? --alias ? ??
    api-key: ""                           # llama.cpp? --api-key ?? ? ??? ? ??
    temperature: 0.2
    max-tokens: 64
    use-guided-json: false
    lora-name: ""
  sse:
    heartbeat-seconds: 20                 # (??) ???? ??

aws:
  region: ap-northeast-2